#!/usr/bin/env ruby

$: << File.expand_path('../../lib', __FILE__)

require 'liquid-proxy/connection'

LIQUID_PROXY_PORT = ARGV[0] || 8998

Proxy.start(:port => LIQUID_PROXY_PORT) do |conn|
  LiquidProxy::Connection.setup(conn)
end
